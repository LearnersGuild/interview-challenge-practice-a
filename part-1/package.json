{
  "name": "section-1",
  "version": "1.0.0",
  "author": "Bonnie Schulkin",
  "scripts": {
    "db:init": "npm run db:create && npm run db:seed",
    "db:create": "psql < schema.sql",
    "db:seed": "psql < seed.sql",
    "db:destroy": "dropdb practice_challenge_v1_flights"
  },
  "dependencies": {
    "pg-promise": "^7.3.2"
  }
}
